# OpenMemory UI Environment Configuration  
# Copy this file to ui.env and fill in your actual values

# API URL (usually localhost when running locally)
NEXT_PUBLIC_API_URL=http://localhost:8765

# User ID (should match the one in api.env)
NEXT_PUBLIC_USER_ID=your-unique-user-id