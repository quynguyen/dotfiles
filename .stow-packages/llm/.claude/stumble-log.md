# Stumble Log

Started 2026-07-29, at the Opus 5 config reset (`pre-opus5-reset` tag in this repo).

One line per correction I have to make that a deleted rule would have prevented. **Two occurrences of the same stumble earns a rule back**, written in the fewest words that fix it. One occurrence is noise.

This file is not auto-loaded. It is read at review time.

Deleted in the reset, so these are the things to watch for:
speculative abstractions and over-engineering; touching adjacent code that wasn't asked about; claiming work is done without running the check; skipping the failing test; leaving lint warnings and dead code behind; jumping to an answer without naming an assumption; Morse-code bullet formatting.

| Date | Stumble | Count |
|---|---|---|
| | | |
